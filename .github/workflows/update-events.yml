name: Update events

on:
  schedule:
    - cron: '0 13 * * *'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      # Pages serves public-site; scrape/commit there so the live site stays fresh.
      - uses: actions/checkout@v4
        with:
          ref: public-site

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Run scraper
        run: node scripts/scrape.js

      # Create-only sync of new future events into Notion (Event Tracker Test by default).
      # Requires repo secret NOTION_TOKEN. Optional: NOTION_DATABASE_ID to retarget.
      - name: Sync new events to Notion
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
          LIMIT: '0'
        run: node scripts/sync-notion.js

      - name: Commit updated events.json to public-site
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/events.json data/geocache.json
          git diff --cached --quiet || git commit -m "Update events.json"
          git push origin HEAD:public-site

      # Keep main's data in sync for Notion/local work on that branch.
      - name: Checkout main for data mirror
        uses: actions/checkout@v4
        with:
          ref: main
          path: main-repo

      - name: Mirror event data to main
        run: |
          cp data/events.json data/geocache.json main-repo/data/
          cd main-repo
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/events.json data/geocache.json
          git diff --cached --quiet || git commit -m "Update events.json"
          git push origin HEAD:main
